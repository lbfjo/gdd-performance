{"PendingMessage":"All done. The spec now includes:\n\n1. **Athlete weekly check-in feedback** — the confirmation screen (step 3) shows the athlete's weekly attendance count and which days they attended (e.g. \"4 of 7 days — Mon · Tue · Wed · Today\").\n\n2. **`date` field on `checkins`** — added `date: string` (Europe/Lisbon, e.g. `\"2026-06-03\"`) for straightforward day-level queries instead of relying on timestamp range queries.\n\n3. **Known Limitations & Future Improvements** — new section at the bottom covering the issues we discussed: PIN hashing weakness, no rate-limiting, missing Firestore Security Rules, composite indexes, denormalised name sync, data retention, timezone/week definition, auto-reset timing, offline behaviour, and free tier budget estimates.","Results":{"spec-update-date":"### `checkins` collection\n```\n{\n  id: string,           // Firestore auto-ID\n  athleteId: string,    // reference to athletes doc\n  athleteName: string,  // denormalised for easy querying\n  date: string,         // \"2026-06-03\" (Europe/Lisbon date, for day-level queries)\n  timestamp: Timestamp  // Firestore server timestamp\n}\n```","spec-limitations":"---\n\n## Known Limitations & Future Improvements\n\nItems identified during design review — acceptable for POC, should be addressed before production:\n\n### Security\n- **PIN hashing:** SHA-256 on a 4-digit PIN (10,000 combinations) is trivially reversible via lookup table. Future: add a per-athlete random salt, or switch to bcrypt/argon2.\n- **No rate-limiting on PIN attempts:** brute-force of all 10,000 PINs is possible in seconds. Future: lockout after 5 failed attempts (30-second cooldown).\n- **Firestore Security Rules not defined:** rules must prevent direct read access to `pin` fields and enforce that only hashed values are written. Must be configured before deployment.\n\n### Data\n- **No composite indexes documented:** queries like \"all check-ins for athlete X in week Y\" need Firestore composite indexes on `athleteId` + `date`.\n- **Denormalised `athleteName` has no sync strategy:** if an athlete's name changes, historical check-in records retain the old name. Acceptable for POC.\n- **No data retention policy:** the `checkins` collection grows unbounded across off-seasons. Low priority for POC.\n\n### UX\n- **Timezone:** all date logic (current day, week boundaries) uses `Europe/Lisbon`. Week is defined as Monday–Sunday (ISO week).\n- **5-second auto-reset:** may be too short for athletes to read their weekly summary. Consider adding a \"Done\" button alongside the timer.\n- **Offline is a blank screen:** without caching the athlete list, the app is non-functional offline — not just \"can't check in\" but no UI at all. The error banner only helps if the list was previously loaded.\n\n### Free tier budget\n- **Firestore reads:** each dashboard load reads ~100 athlete docs + ~500 check-in docs (100 athletes × ~5 check-ins/week). 10 coaches refreshing a few times daily consumes ~10K–15K of the 50K daily read limit. Sufficient but not as generous as \"more than sufficient\" implies.","spec-update-confirmation":"3. **Confirmation** — success screen showing athlete name, timestamp, and weekly attendance summary. Auto-resets to step 1 after 5 seconds.\n   - Weekly summary shows: current count (e.g. \"4 of 7 days\"), and which days were attended (e.g. Mon · Tue · Wed · Today)\n   - Data sourced from the same `checkins` collection (athlete's check-ins filtered by current week)\n   - Read-only — no editing or history beyond current week"},"Memory":{"__ground_next_attempts":{"$type":"<>f__AnonymousType9`2[[System.Object, System.Private.CoreLib],[System.String, System.Private.CoreLib]], Orchestrator.Core","value":1,"kind":"system"},"__already_hydrated__":{"$type":"<>f__AnonymousType9`2[[System.Object, System.Private.CoreLib],[System.String, System.Private.CoreLib]], Orchestrator.Core","value":true,"kind":"native"},"__pending_plan__":{"$type":"<>f__AnonymousType9`2[[System.Object, System.Private.CoreLib],[System.String, System.Private.CoreLib]], Orchestrator.Core","value":"Yes — the athlete should see their own weekly check-in count after checking in. It's a natural feedback loop and costs almost nothing to add.\n\n**Why it matters:**\n\n- **Motivation.** Seeing \"4/6 this week\" reinforces the habit. Without it, the athlete taps a button and gets a generic \"success\" with no sense of progress.\n- **Self-correction.** If they think they already came 3 times but the app shows 2, they can flag it to the coach immediately — rather than discovering discrepancies on the leaderboard later.\n- **Reduces coach load.** Athletes stop asking \"how many times did I come this week?\" — they can see it themselves.\n\n**What I'd add to the spec:**\n\nAfter a successful check-in, the confirmation screen (the 5-second auto-reset one) should show:\n\n> **Check-in registered!**\n> This week: **4 of 6 days** (Mon · Tue · Wed · Today)\n\n- Show the count and which days they checked in (dots or abbreviated day names).\n- This data is already being queried for the coach's weekly grid — it's the same `checkins` collection filtered by athlete + current week. No new data model needed.\n- Keep it read-only on the athlete side — no editing, no history beyond current week.\n\n**Cost:** One additional Firestore query per check-in (athlete's check-ins for current week) — roughly 0–6 extra reads per check-in. Negligible against the free tier budget.","kind":"native"},"__dreams_enabled__":{"$type":"<>f__AnonymousType9`2[[System.Object, System.Private.CoreLib],[System.String, System.Private.CoreLib]], Orchestrator.Core","value":true,"kind":"native"},"__last_phase":{"$type":"<>f__AnonymousType9`2[[System.Object, System.Private.CoreLib],[System.String, System.Private.CoreLib]], Orchestrator.Core","value":"ground_next","kind":"system"}},"Artifacts":[],"InspectionRequests":[],"PinnedSearch":null,"LastSearchResults":null,"IterationNumber":3,"Plan":null,"Focus":null,"Todos":[]}